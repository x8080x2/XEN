        services:
        - type: web
          name: your-app-name
          env: node
          buildCommand: |
            npm install
            npx puppeteer browsers install chrome --path /opt/render/.cache/puppeteer
            npm run build
          startCommand: npm run start