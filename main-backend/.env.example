# Main Backend Environment Variables

# JWT Configuration
JWT_SECRET=your-main-backend-jwt-secret-here

# API Keys
DEFAULT_API_KEY=admin-api-key-2024

# Server Configuration
PORT=4000
NODE_ENV=production
MAIN_BACKEND_VERSION=1.0.0

# CORS Configuration
ALLOWED_ORIGINS=https://your-client-domain.com,http://localhost:3000

# Database Configuration (if using external DB)
# DATABASE_URL=postgresql://user:password@host:port/database

# Email Service Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password